<UserControl x:Class="Monolith.MonolithSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="20">
        <TextBlock Text="Game Directories (One path per line):" Margin="0,0,0,5" FontWeight="Bold"/>
        <TextBox Text="{Binding ScanDirectoriesText}" 
                 AcceptsReturn="True" 
                 Height="200" 
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,20"/>

        <TextBlock Text="Other Settings (Example):"/>
        <CheckBox Content="Example Option 2" IsChecked="{Binding Settings.Option2}" Margin="0,5,0,0"/>
    </StackPanel>
</UserControl>