<UserControl x:Class="Monolith.MonolithSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <CheckBox Content="Auto-scan for new games on startup" 
                      IsChecked="{Binding Settings.AutoScanOnStartup}" 
                      Margin="0,0,0,15"
                      FontWeight="Bold"/>

            <TextBlock Text="Game Directories (One path per line):" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox Text="{Binding ScanDirectoriesText, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" 
                     Height="120" 
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,20"/>

            <TextBlock Text="Skip List / Blacklist (File extensions or names to ignore):" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox Text="{Binding SkipListText, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" 
                     Height="100" 
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,10"/>
            <TextBlock Text="Tips: You can add 'dxsetup.exe', '.tmp', or specific folder names here." 
                       FontSize="10" 
                       FontStyle="Italic" 
                       Opacity="0.7"
                       Margin="0,0,0,20"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>